import {generateSecureAI}from'./secureAIService';
function json<T>(text:string,fallback:T):T{try{const cleaned=text.replace(/```json\s*/gi,'').replace(/```/g,'').trim();const match=cleaned.match(/\{[\s\S]*\}|\[[\s\S]*\]/);return match?JSON.parse(match[0]):fallback}catch{return fallback}}
export async function extraireTexteCours(imageBase64:string,matiere:string,mimeType='image/jpeg'){const content=await generateSecureAI({messages:[{role:'user',content:`Analyse ce support de cours de ${matiere}. Extrais uniquement le contenu pédagogique utile pour créer des questions. Si c'est une image, lis les éléments visibles. Si c'est un PDF, exploite son contenu. Réponds en français.`}],imageBase64,mimeType,temperature:.2,max_tokens:2200});return content}
export async function genererQuestionsCours(contenu:string,matiere:string){const raw=await generateSecureAI({messages:[{role:'user',content:`À partir de ce contenu de ${matiere}, crée 8 questions de révision adaptées à un élève du secondaire. Ne fabrique pas de notions absentes du contenu. Retourne uniquement JSON sous la forme {"questions":[{"texte":"...","reponseAttendue":"...","criteresCorrection":"..."}]}. CONTENU:\n${contenu}`}],temperature:.2,max_tokens:1800});const data=json<{questions:any[]}>(raw,{questions:[]});return Array.isArray(data.questions)?data.questions:[]}
export async function evaluerReponseRevision(question:string,reponse:string,essai:number,matiere:string,contenuCours?:string,reponseAttendue?:string,criteresCorrection?:string){const raw=await generateSecureAI({messages:[{role:'user',content:`Évalue cette réponse d'élève en ${matiere}. Note 0, 1 ou 2. Question: ${question}\nRéponse: ${reponse}\nRéponse attendue: ${reponseAttendue||'non fournie'}\nCritères: ${criteresCorrection||'non fournis'}\nCours de référence: ${contenuCours||'non fourni'}\nEssai ${essai}. Retourne uniquement JSON {"note":0,"feedback":"..."}.`}],temperature:.1,max_tokens:500});const data=json<{note:number;feedback:string}>(raw,{note:0,feedback:'Impossible d’analyser la réponse.'});return{note:Math.max(0,Math.min(2,Number(data.note)||0)),feedback:data.feedback||'Analyse terminée.'}}
