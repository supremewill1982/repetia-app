[1mdiff --git a/App.tsx b/App.tsx[m
[1mindex 993a415..469178d 100644[m
[1m--- a/App.tsx[m
[1m+++ b/App.tsx[m
[36m@@ -1,44 +1,54 @@[m
[32m+[m[32mimport { useAppTimeTracking } from './src/hooks/useAppTimeTracking';[m
 import React from 'react';[m
 import { StatusBar } from 'expo-status-bar';[m
 import { NavigationContainer } from '@react-navigation/native';[m
 import { ThemeProvider, useTheme } from './src/context/ThemeContext';[m
 import { AuthProvider, useAuth } from './src/context/AuthContext';[m
[32m+[m
 import AuthNavigator from './src/navigation/AuthNavigator';[m
 import RepetiteurNavigator from './src/navigation/RepetiteurNavigator';[m
 import EtablissementNavigator from './src/navigation/EtablissementNavigator';[m
 import AdminNavigator from './src/navigation/AdminNavigator';[m
 import ParentNavigator from './src/navigation/ParentNavigator';[m
 import EleveNavigator from './src/navigation/EleveNavigator';[m
[32m+[m
 import { View, ActivityIndicator } from 'react-native';[m
 [m
[31m-// Composant principal de l'application[m
 function AppContent() {[m
[31m-  const { user, loading } = useAuth();[m
[32m+[m[32m  useAppTimeTracking();[m
[32m+[m[32m  const { user, userRole, loading } = useAuth();[m
   const { colors } = useTheme();[m
 [m
   if (loading) {[m
     return ([m
[31m-      <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center', backgroundColor: colors.background }}>[m
[31m-        <ActivityIndicator size="large" color={colors.primary} />[m
[32m+[m[32m      <View[m
[32m+[m[32m        style={{[m
[32m+[m[32m          flex: 1,[m
[32m+[m[32m          justifyContent: 'center',[m
[32m+[m[32m          alignItems: 'center',[m
[32m+[m[32m          backgroundColor: colors.background,[m
[32m+[m[32m        }}[m
[32m+[m[32m      >[m
[32m+[m[32m        <ActivityIndicator[m
[32m+[m[32m          size="large"[m
[32m+[m[32m          color={colors.primary}[m
[32m+[m[32m        />[m
       </View>[m
     );[m
   }[m
 [m
   return ([m
     <>[m
[31m-      <StatusBar style={colors.mode === 'dark' ? 'light' : 'dark'} />[m
[32m+[m[32m      <StatusBar style="auto" />[m
[32m+[m
       <NavigationContainer>[m
         {user ? ([m
[31m-          user.role === 'admin' ? ([m
[31m-            <AdminNavigator />[m
[31m-          ) : user.role === 'etablissement' ? ([m
[31m-            <EtablissementNavigator />[m
[31m-          ) : user.role === 'repetiteur' ? ([m
[31m-            <RepetiteurNavigator />[m
[31m-          ) : user.role === 'parent' ? ([m
[32m+[m[32m          userRole === 'parent' ? ([m
             <ParentNavigator />[m
[32m+[m[32m          ) : userRole === 'eleve' ? ([m
[32m+[m[32m            <EleveNavigator />[m
           ) : ([m
[31m-            <EleveNavigator />  // ✅ Rôle par défaut : élève[m
[32m+[m[32m            <EleveNavigator />[m
           )[m
         ) : ([m
           <AuthNavigator />[m
[36m@@ -48,7 +58,6 @@[m [mfunction AppContent() {[m
   );[m
 }[m
 [m
[31m-// Composant racine avec les providers[m
 export default function App() {[m
   return ([m
     <ThemeProvider>[m
[1mdiff --git a/src/hooks/useAppTimeTracking.ts b/src/hooks/useAppTimeTracking.ts[m
[1mindex 25ff158..9ea2e73 100644[m
[1m--- a/src/hooks/useAppTimeTracking.ts[m
[1m+++ b/src/hooks/useAppTimeTracking.ts[m
[36m@@ -1,4 +1,5 @@[m
 import { useEffect, useRef } from 'react';[m
[32m+[m[32mimport { useAuth } from '../context/AuthContext';[m
 import { AppState, AppStateStatus } from 'react-native';[m
 import {[m
   startTimeTracking,[m
[36m@@ -7,6 +8,7 @@[m [mimport {[m
 } from '../services/timeTrackingService';[m
 [m
 export function useAppTimeTracking() {[m
[32m+[m[32m  const { user } = useAuth();[m
   const appState   = useRef<AppStateStatus>(AppState.currentState);[m
   const mounted    = useRef(true);[m
   const intervalRef = useRef<ReturnType<typeof setInterval> | null>(null);[m
[36m@@ -16,6 +18,7 @@[m [mexport function useAppTimeTracking() {[m
 [m
     // ── Démarrage initial[m
     const init = async () => {[m
[32m+[m[32m      if (!user) return;[m
       if (AppState.currentState === 'active') {[m
         await _demarrerNavigation();[m
       }[m
[36m@@ -30,6 +33,8 @@[m [mexport function useAppTimeTracking() {[m
       const prevState = appState.current;[m
       appState.current = nextState;[m
 [m
[32m+[m[32m      if (!user) return;[m
[32m+[m
       if (prevState !== 'active' && nextState === 'active') {[m
         // ✅ Retour sur l'app (depuis verrou, autre app, background)[m
         await _demarrerNavigation();[m
[36m@@ -41,7 +46,7 @@[m [mexport function useAppTimeTracking() {[m
 [m
     // ── Polling léger (5s) : assure cohérence si session externe terminée[m
     intervalRef.current = setInterval(async () => {[m
[31m-      if (!mounted.current || AppState.currentState !== 'active') return;[m
[32m+[m[32m      if (!mounted.current || !user || AppState.currentState !== 'active') return;[m
       const stats = await getTimeStats();[m
       // Si aucune session active et app au premier plan → démarrer navigation[m
       if (!stats.currentSession?.type) {[m
[36m@@ -56,7 +61,7 @@[m [mexport function useAppTimeTracking() {[m
       // ✅ Flush propre à la destruction du composant[m
       _stopperTracking();[m
     };[m
[31m-  }, []);[m
[32m+[m[32m  }, [user]);[m
 }[m
 [m
 async function _demarrerNavigation() {[m
